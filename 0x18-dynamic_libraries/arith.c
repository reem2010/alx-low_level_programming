
/**
 * add - add two numbers
 * @x: first numbrt
 * @y: second number
 * Return: result
 */
int add(int x, int y)
{
	int res = 0;

	res = x + y;
	return (res);
}
/**
 * sub - sub two numbers
 * @x: first numbrt
 * @y: second number
 * Return: result
 */
int sub(int x, int y)
{
	int res = 0;

	res = x - y;
	return (res);
}
/**
 * mul - mul two numbers
 * @x: first numbrt
 * @y: second number
 * Return: result
 */
int mul(int x, int y)
{
	int res = 0;

	res = x * y;
	return (res);
}
/**
 * div - divide two numbers
 * @x: first numbrt
 * @y: second number
 * Return: result
 */
int div(int x, int y)
{
	int res = 0;

	res = x / y;
	return (res);
}
/**
 * mod - mod two numbers
 * @x: first numbrt
 * @y: second number
 * Return: result
 */
int mod(int x, int y)
{
	int res = 0;

	res = x % y;
	return (res);
}
